# Fixture: supported matrix that is very old. The doctor must surface a
# staleness warning, and when combined with an unknown component version
# it must escalate to a blocker.
schema_version: 1
staleness_window_days: 30

components:
  - component: vllm
    minimum_version: "0.1.0"
    known_bad_ranges: []
    verified_at: "2020-01-01"
    verified_sources: ["https://example.test/vllm"]
