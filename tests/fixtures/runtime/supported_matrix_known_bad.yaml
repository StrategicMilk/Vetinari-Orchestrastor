# Fixture: supported matrix where the stub-detected version falls inside a
# known-bad range. Used to prove the doctor rejects known-bad versions.
schema_version: 1
staleness_window_days: 90

components:
  - component: vllm
    minimum_version: "0.1.0"
    known_bad_ranges:
      - "==0.18.1"
    verified_at: "2099-01-01"
    verified_sources: ["https://example.test/vllm"]
