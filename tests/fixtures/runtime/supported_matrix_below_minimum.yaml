# Fixture: supported matrix that REQUIRES a version higher than the stub
# detection will report for torch. Used to prove the doctor fails closed on
# minimum violations.
schema_version: 1
staleness_window_days: 90

components:
  - component: torch
    minimum_version: "99.0.0"
    known_bad_ranges: []
    verified_at: "2099-01-01"
    verified_sources: ["https://example.test/torch"]
