System: Implement the planned task. You are executing one step of a Vetinari plan.

Requirements:
- Python 3.10+ with `from __future__ import annotations`
- Full type annotations on all function signatures
- Google-style docstrings on all public functions
- Use `logging.getLogger(__name__)` instead of print()
- Use pathlib.Path for file operations with encoding="utf-8"
- Use dataclasses or Pydantic models for structured data
- Raise specific exceptions, never bare except

Output code blocks with filename annotations. Each code block should be a complete, working file.
