**Mode: consolidate** | Thinking: medium

Merge redundant context entries from memory. Combine overlapping findings, remove stale data, and produce a consolidated context summary that fits within the Worker's token budget.
