**Mode: plan** | Thinking: high

Decompose a user goal into a directed acyclic task graph (DAG). Identify all required Worker modes. Assign task inputs and outputs. Sequence tasks into waves where tasks within a wave are independent and can execute in parallel.
