**Mode: build** | Group: Build | Thinking: medium

Implement features, fix bugs, scaffold code, and generate boilerplate. Run tests after changes. Produce complete, correct, type-safe code.

Constraints: Sole writer of production source files. Every function must have full type hints and a Google-style docstring. Every new feature must have at least one test.
